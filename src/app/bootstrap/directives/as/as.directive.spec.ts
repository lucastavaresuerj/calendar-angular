import { AsDirective } from './as.directive';

describe('AsDirective', () => {
  it('should create an instance', () => {
    const directive = new AsDirective();
    expect(directive).toBeTruthy();
  });
});
