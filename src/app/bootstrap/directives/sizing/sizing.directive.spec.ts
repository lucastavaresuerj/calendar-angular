import { SizingDirective } from './sizing.directive';

describe('SizingDirective', () => {
  it('should create an instance', () => {
    const directive = new SizingDirective();
    expect(directive).toBeTruthy();
  });
});
